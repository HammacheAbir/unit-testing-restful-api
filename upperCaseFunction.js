export const toUpperCaseFunction = (name)=>{
    if(name==="abir"){
        return "The upper case of name is "+name
    }else{
        return "The upper case of name is "+name.toUpperCase()
    }
    
}